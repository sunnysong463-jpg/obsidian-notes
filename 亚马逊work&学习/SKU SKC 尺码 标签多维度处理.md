2026-06-03
SKU SKC 尺码 标签多维度处理

问题：
**同一款号 + 颜色（同一 SKC）下，不同尺码的 SKC 标签不统一**，问题的本质 ——SKC 维度的标签一致性缺失，而非零散的 SKU 标签问题
- 清晰拆解处理任务：明确了「先找出所有标签不统一的 SKC，再按规则统一标签」

我有一份亚马逊睡衣表，包含字段：[款号]、[SKU]、[颜色]、[尺码]、[SKC标签]。 当前问题： > 存在逻辑冲突。同一个“款号+颜色”（即同一个 SKC）下，不同尺码的 [SKC标签] 不统一（例如有的尺码是“四季款”，有的是“已下架”）。
处理任务： 请找出所有 [SKC标签] 不统一的 SKC。 统一这些标签。统一逻辑为：如果该 SKC 下有任何一个尺码标记为“四季款”，则该 SKC 下所有 SKU 均统一为“四季款”；在处理“标签不一”时，告诉 AI 一个==优先级逻辑==非常重要。例如：==四季款 > 夏季＞售完下架 > 已下架==。这样 AI 在自动合并时，会自动选择权重最高的标签，而不是随机选择。 请直接输出处理后的表格数据，保持原有的列结构。


是否需要保留原始数据备份」「是否需要单独输出冲突 SKC 明细报表」
![[处理后_SKC标签修正_豆包AI生成.xlsx]]

==遇到问题先问ai：如何拆解处理任务，核心处理规则，输出边界要求等==